import { PagePlaceholder } from "../components/common/PagePlaceholder";

/** Fase 6: watchlist, umbral de señal, params Markov (n auto/manual con BIC), Telegram. */
export function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      phase="Fase 6"
      description="Watchlist de símbolos, umbral de emisión, número de estados del modelo Markov/HMM (auto por BIC o manual) y configuración de alertas Telegram."
    />
  );
}
