import { PagePlaceholder } from "../components/common/PagePlaceholder";

/** Fase 6: desglose de los 5 componentes del KPI + estado del HMM (n, BIC). */
export function AnalysisPage() {
  return (
    <PagePlaceholder
      title="Analysis"
      phase="Fase 6"
      description="Régimen y matriz de transición, técnicos, microestructura, derivados y sentimiento — con la contribución de cada uno al KPI de Seguridad."
    />
  );
}
